#include <stdio.h>

int main(void) {
    typedef struct {
        int a;
        char b;
    } Struct_Personalizada;

    Struct_Personalizada aux, cod, taxa;
    aux = cod;
    taxa = cod;

    return 0;
}